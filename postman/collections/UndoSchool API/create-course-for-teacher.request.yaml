$kind: http-request
method: POST
url: '{{baseUrl}}/api/teachers/{{teacherId}}/courses'
order: 5000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "{{name}}",
      "description": "{{desc}}"
    }
