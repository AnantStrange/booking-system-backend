$kind: http-request
method: POST
url: '{{baseUrl}}/api/parents'
order: 8000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "{{name}}",
      "timezone": "{{timezone}}"
    }
